{
    "version":2,
    "builds": [
        {"src":"app.py", "use":"@vercel/python"}{
            "maxDuration" : 5
        }
    ],
    "routes":[
        {"src":"/(.*)", "dest":"app.py"}
    ]
}
